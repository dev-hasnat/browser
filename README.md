# Desktop View Browser

A lightweight mobile browser built with Flutter that forces desktop view using a custom user-agent.

## Features

- Loads websites in full desktop view
- URL input and bookmarks
- Back/Forward navigation
